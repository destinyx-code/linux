#include <stdio.h>
	main() {
		char os;
		char windows[]; = "Windows";
		char macos[] = "MacOs";
		char gnulinux[] = "Gnu/Linux";
		char none;
			printf("What Os would you like to run with Linux?");
				scanf("%d", &os);
					if (os == windows) {
						
					}
	}
return 0;
