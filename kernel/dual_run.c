#include <stdio.h>
	main() {
		str os;
		str windows = "Windows";
		str macos = "MacOs";
		str gnulinux = "Gnu/Linux";
		str none;
			printf("What Os would you like to run with Linux?");
				scanf("%d", &os);
					if (os == windows) {
						
					}
	}
return 0;
